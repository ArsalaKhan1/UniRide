import { defineConfig } from 'vite'

// Minimal Vite config - plugin removed to avoid ESM/require issues in this environment.
// If you want React fast-refresh and other React-specific features, re-add
// `@vitejs/plugin-react` after ensuring your Node/Vite setup supports ESM config.
export default defineConfig({})
